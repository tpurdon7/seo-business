{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Better Search Audit window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
